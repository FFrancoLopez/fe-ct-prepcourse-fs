function ordenarArray(array) {
  // Ordena los elementos del areglo array de menor a mayor.
  // Devuelve el arreglo resultante.
  // Tu código:
  // Usando el metodo .sort() ordenamos los elementos del array.
  // El modo de ordenación por defecto responde a la posición de acuerdo a su valor <<"Unicode">>.
  array.sort();
  return array;
}

module.exports = ordenarArray;
