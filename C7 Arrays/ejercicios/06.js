function invertirArray(array) {
  // Invierte el arreglo array recibido por argumento.
  // Tu código:
  // Usamos el metodo .reverse() para invertir los elementos del array y retornar el array actualizado.
  array.reverse();
  return array;
}

module.exports = invertirArray;
