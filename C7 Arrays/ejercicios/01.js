function devolverPrimerElemento(array) {
  // Retornar el primer elemento del arreglo recibido.
  // Tu código:
  // Retornamos el elemento que se encuentra en la posicion 0 (primer indice).
  return array[0];
}

module.exports = devolverPrimerElemento;
